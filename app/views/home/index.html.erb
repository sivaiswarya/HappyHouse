<div class="row">
  <div class="col-xs-4">
    <h1>Categories</h1>
    <div class="list-group">
      <% @categories.each do |category| %>
        <%= link_to category[1], category_products_path(URI::encode(category[1])), class: "list-group-item" %>
      <% end %>
    </div>
  </div>
  <div class="col-xs-8">
    <h1>Featured Products</h1>
    <div><p>TODO: Carousel</p></div>
  </div>
</div>